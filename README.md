# Installation

`git clone https://github.com/joshuazd/dotfiles.git`
`./dotfiles/install.sh`

This will clone the repository into the dotfiles folder, and run the installation script
The bashrc, zshrc, inputrc, tmux.conf, aliases, functions, and vim config will be backed up before installing each file
